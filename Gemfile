source 'http://rubygems.org'

gem 'bson_ext'
gem 'mongoid'
gem 'rails', '3.2.6'
gem 'cancan' # Must come after mongoid

group :assets do
  gem 'coffee-rails'
  gem 'sass'
  gem 'sass-rails'
  gem 'uglifier'
end

group :development do
end

group :development, :test do
end